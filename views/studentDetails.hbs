<div class="d-flex justify-content-between align-items-center mb-3">
    <a href="/" class="text-decoration-none">&larr; Back to list</a>
    <div>
        <a class="btn btn-sm btn-outline-primary" href="/students/{{student.id}}/edit">Edit</a>
        <form method="POST" action="/students/{{student.id}}?_method=DELETE" class="d-inline"
              onsubmit="return confirm('Delete student #{{student.id}}?');">
            <button class="btn btn-sm btn-outline-danger">Delete</button>
        </form>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <h1 class="h4 mb-3">Student #{{student.StudentID}}</h1>
        <dl class="row">
            <dt class="col-sm-3">First Name</dt>
            <dd class="col-sm-9">{{student.FirstName}}</dd>

            <dt class="col-sm-3">Last Name</dt>
            <dd class="col-sm-9">{{student.LastName}}</dd>

            <dt class="col-sm-3">Major</dt>
            <dd class="col-sm-9">{{student.Major}}</dd>

            <dt class="col-sm-3">GPA</dt>
            <dd class="col-sm-9">{{student.GPA}}</dd>
        </dl>
    </div>
</div>